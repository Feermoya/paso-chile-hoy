---
/**
 * Bloque de alertas por paso desde X: antes leía `tweets.json` generado por scraping.
 * Ese flujo se eliminó. Si en el futuro querés alertas por paso, conviene un JSON manual dedicado.
 */
return;
---
